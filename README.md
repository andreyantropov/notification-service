# 📣 Notification Platform

**Монорепозиторий** для микросервисной платформы уведомлений.  
Отправка транзакционных сообщений через **Bitrix24**, **Email** и другие каналы — с поддержкой повторных попыток, отказоустойчивости и полной наблюдаемости.

> 🧩 Состоит из переиспользуемых пакетов и независимых сервисов, объединённых общим ядром (`shared`).

---

## 🏗️ Структура проекта

```
notification-platform/
├── shared/                 # Shared Kernel (DDD)
├── packages/               # Библиотеки: http, queues, telemetry
├── services/               # Микросервисы: notification-service, retry-service
├── infra/                  # Конфигурации RabbitMQ, Nginx, Keycloak
├── docs/                   # Архитектура, ADR, гайдлайны
└── docker-compose.yml      # Локальное развёртывание
```

---

## ⚙️ Требования

| Компонент          | Версия               |
| ------------------ | -------------------- |
| **Node.js**        | ≥ 23.0.0             |
| **npm**            | ≥ 9.0.0              |
| **Docker + Compose** | Для локального запуска |

---

## 🚀 Быстрый старт

```bash
npm install
docker-compose up --build
```

> Healthcheck: `GET /api/health/live`, `GET /api/health/ready`  
> Конфигурация — через `.env` (см. `*.example` в папках сервисов).

---

## 📚 Документация

- [Архитектура](./docs/architecture/architecture.md)
- [ADR](./docs/adr/)
- [Соглашения по коду](./docs/guideline.md)

---

## 🐳 Docker

Платформа собирается и запускается локально через `docker-compose`.  
Подробная инструкция — в [docs/docker.md](./docs/docker.md).

---

> ✨ Проект следует принципам **чистой архитектуры**, **явной конфигурации** и **отказа от магии**.
```