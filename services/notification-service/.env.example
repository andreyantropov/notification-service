# ===========================================================================
# 🔧 SERVICE — основные метаданные и настройки HTTP-сервера
# ===========================================================================

# Имя сервиса в инфраструктуре (обязательно)
# Используется в логах, трейсах, метриках, docker
SERVICE_NAME=notification-service

# Версия приложения (обязательно)
# Рекомендуется синхронизировать с package.json
SERVICE_VERSION=1.0.0

# Окружение выполнения: development | staging | production (обязательно)
SERVICE_ENVIRONMENT=development

# Порт HTTP-сервера (опционально, по умолчанию 3000)
# ⚠️ Должен совпадать с EXPOSE в Dockerfile
SERVICE_PORT=3000

# Публичный базовый URL API (опционально, по умолчанию http://localhost:3000/api)
# Используется в OpenAPI-спецификации
SERVICE_PUBLIC_URL=http://localhost:3000/api

# Заголовок API в Swagger UI (обязательно)
SERVICE_TITLE=Notification Service API

# Описание API в Swagger UI (опционально)
SERVICE_DESCRIPTION=API сервиса отправки транзакционных уведомлений


# ===========================================================================
# 📡 BITRIX24 CHANNEL — настройки подключения к Bitrix24 REST API
# ⚠️ По умолчанию канал ВКЛЮЧЁН. Чтобы отключить — укажите BITRIX_CHANNEL_IS_ENABLED=false
# ===========================================================================

# Управляющий флаг: включить/отключить Bitrix-канал (опционально, по умолчанию true)
# Допустимые значения: "true" или "false"
BITRIX_CHANNEL_IS_ENABLED=true

# Базовый URL портала Bitrix24 (обязательно)
BITRIX_CHANNEL_BASE_URL=https://bitrix24.ru

# ID пользователя от имени которого отправляются уведомления (обязательно)
BITRIX_CHANNEL_USER_ID=1

# OAuth-токен или вебхук-токен для аутентификации (обязательно)
BITRIX_CHANNEL_AUTH_TOKEN=qwerty

# Таймаут на отправку одного уведомления в Bitrix24 (мс, опционально, по умолчанию 10_000)
BITRIX_CHANNEL_SEND_TIMEOUT_MS=10000

# Таймаут на проверку доступности Bitrix24 в healthcheck (мс, опционально, по умолчанию 5_000)
BITRIX_CHANNEL_HEALTHCHECK_TIMEOUT_MS=5000


# ===========================================================================
# 📧 EMAIL CHANNEL — настройки SMTP для отправки email
# ⚠️ По умолчанию канал ВКЛЮЧЁН. Чтобы отключить — укажите EMAIL_CHANNEL_IS_ENABLED=false
# ===========================================================================

# Управляющий флаг: включить/отключить Email-канал (опционально, по умолчанию true)
# Допустимые значения: "true" или "false"
EMAIL_CHANNEL_IS_ENABLED=true

# SMTP-хост (обязательно)
EMAIL_CHANNEL_HOST=mail.ru

# SMTP-порт (опционально, по умолчания 25)
EMAIL_CHANNEL_PORT=25

# Использовать TLS/SSL (опционально, по умолчанию false)
EMAIL_CHANNEL_SECURE=false

# Логин для аутентификации в SMTP (обязательно)
EMAIL_CHANNEL_LOGIN=user

# Пароль для аутентификации в SMTP (обязательно)
EMAIL_CHANNEL_PASSWORD=qwerty

# Адрес отправителя в формате: "Имя" <email@example.com> (обязательно)
EMAIL_CHANNEL_FROM_EMAIL="Example" <no-reply@example.com>

# Таймаут на установку SMTP-соединения (мс, опционально, по умолчанию 5_000)
EMAIL_CHANNEL_GREETING_TIMEOUT_MS=5000

# Таймаут на отправку одного email (мс, опционально, по умолчанию 10_000)
EMAIL_CHANNEL_SEND_TIMEOUT_MS=10000

# Таймаут на проверку SMTP в healthcheck (мс, опционально, по умолчанию 5_000)
EMAIL_CHANNEL_HEALTHCHECK_TIMEOUT_MS=5000


# ===========================================================================
# ⚙️ CONTROLLERS — таймауты обработки запросов
# ===========================================================================

# Максимальное время обработки запроса на отправку уведомления (мс, опционально, по умолчанию 60_000)
NOTIFICATION_CONTROLLER_SEND_TIMEOUT_MS=60000

# Таймаут проверки готовности зависимостей в /health/ready (мс, опционально, по умолчанию 5_000)
HEALTHCHECK_CONTROLLER_READY_TIMEOUT_MS=10000


# ===========================================================================
# 🐇 RABBITMQ — настройки брокера сообщений
# ===========================================================================

# AMQP-URL подключения к RabbitMQ (обязательно)
RABBITMQ_URL=amqp://guest:guest@localhost:5672/


# ===========================================================================
# 📤 PRODUCER — настройки отправки сообщений в очередь
# ===========================================================================

# Имя основной очереди уведомлений (обязательно)
PRODUCER_QUEUE=notifications

# Таймаут публикации сообщения в очередь (мс, опционально, по умолчанию 10_000)
PRODUCER_PUBLISH_TIMEOUT_MS=10000

# Таймаут проверки подключения к RabbitMQ в healthcheck (мс, опционально, по умолчанию 5_000)
PRODUCER_HEALTHCHECK_TIMEOUT_MS=5000


# ===========================================================================
# 📥 BATCH CONSUMER — обработка уведомлений пачками
# ===========================================================================

# Имя очереди для потребителя пакетной обработки (обязательно)
BATCH_CONSUMER_QUEUE=notifications

# Максимальное количество уведомлений в одной пачке (опционально, по умолчанию 100)
BATCH_CONSUMER_MAX_BATCH_SIZE=100

# Интервал авто-сброса буфера, даже если пачка не заполнена (мс, опционально, по умолчанию 60_000)
BATCH_CONSUMER_BATCH_FLUSH_TIMEOUT_MS=60000

# При ошибке — возвращать сообщение в очередь (опционально, по умолчанию false)
BATCH_CONSUMER_REQUEUE=false

# Подтверждать несколько сообщений за раз (опционально, по умолчанию false)
BATCH_CONSUMER_MULTIPLE=false

# Таймаут на отправку пачки уведомлений (мс, опционально, по умолчанию 10_000)
BATCH_CONSUMER_FLUSH_TIMEOUT_MS=10000

# Таймаут healthcheck для потребителя (мс, опционально, по умолчанию 5_000)
BATCH_CONSUMER_HEALTHCHECK_TIMEOUT_MS=5000


# ===========================================================================
# 🔐 AUTHENTICATION — настройки OAuth2/JWT (OIDC)
# ⚠️ По умолчанию middleware ВКЛЮЧЁН. Чтобы отключить — укажите AUTHENTICATION_MIDDLEWARE_IS_ENABLED=false
# ===========================================================================

# Управляющий флаг: включить/отключить middleware (опционально, по умолчанию true)
# Допустимые значения: "true" или "false"
AUTHENTICATION_MIDDLEWARE_IS_ENABLED=true

# URL эндпоинта OpenID Connect Issuer (обязательно)
AUTHENTICATION_MIDDLEWARE_ISSUER=https://keycloak.ru/realms/internal

# URL JWKS-эндпоинта для проверки подписи токена (обязательно)
AUTHENTICATION_MIDDLEWARE_JWKS_URI=https://keycloak.ru/realms/internal/protocol/openid-connect/certs

# Ожидаемый audience в JWT (обязательно)
AUTHENTICATION_MIDDLEWARE_AUDIENCE=notification-service

# Алгоритм подписи токена (опционально, по умолчанию RS256)
AUTHENTICATION_MIDDLEWARE_TOKEN_SIGNING_ALG=RS256


# ===========================================================================
# 🔑 AUTHORIZATION — проверка ролей
# ⚠️ По умолчанию middleware ВКЛЮЧЁН. Чтобы отключить — укажите AUTHORIZATION_MIDDLEWARE_IS_ENABLED=false
# ===========================================================================

# Управляющий флаг: включить/отключить middleware (опционально, по умолчанию true)
# Допустимые значения: "true" или "false"
AUTHORIZATION_MIDDLEWARE_IS_ENABLED=true

# Client ID сервиса в Keycloak (обязательно)
AUTHORIZATION_MIDDLEWARE_SERVICE_CLIENT_ID=notification-service

# Минимальные роли, необходимые для доступа к API (обязательно)
# Указывается через запятую при поддержке множества ролей (например: notifications:send,admin)
AUTHORIZATION_MIDDLEWARE_REQUIRED_ROLES=notifications:send


# ===========================================================================
# 📊 TELEMETRY — экспорт телеметрии в OpenTelemetry Collector
# ===========================================================================

# Эндпоинт для отправки трейсов (опционально)
TELEMETRY_TRACES_EXPORTER_URL=http://otel-collector:4318/v1/traces

# Эндпоинт для отправки логов (опционально)
TELEMETRY_LOGS_EXPORTER_URL=http://otel-collector:4318/v1/logs

# Эндпоинт для отправки метрик (опционально)
TELEMETRY_METRICS_EXPORTER_URL=http://otel-collector:4318/v1/metrics