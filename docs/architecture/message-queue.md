# 🐇 Очередь сообщений

Этот документ описывает, как платформа уведомлений использует **RabbitMQ** для надёжной, отказоустойчивой асинхронной обработки несрочных уведомлений с поддержкой **автоматических повторных попыток**.

Вся логика работы с очередями инкапсулирована в пакете [`@notification-platform/queues`](../../packages/queues), который предоставляет типобезопасные реализации `Producer` и `Consumer`, а также декораторы с логированием.

---

## Обзор

- **`notification-service`**:
  - **Публикует** несрочные уведомления в очередь `notifications`,
  - **Потребляет** их через `BatchConsumer` (обработка пачками).

- **`retry-service`**:
  - **Потребляет** сообщения из очереди `notifications.retry.router`,
  - Маршрутизирует их в очереди с задержкой или в DLQ.

При ошибках обработки в `notification-service` сообщение автоматически направляется в **`notifications.retry.router`**.  
`retry-service` читает оттуда и направляет сообщение:
- в `notifications.retry.30m` (задержка 30 минут),
- в `notifications.retry.2h` (задержка 2 часа),
- или в `notifications.dlq` (после 2 неудачных попыток).

Очереди с TTL автоматически возвращают сообщения в `notifications` по истечении времени.

Это обеспечивает **гарантированную доставку** с экспоненциальной задержкой и защиту внешних систем от флуда.

> 💡 Каждое уведомление обрабатывается **максимум 3 раза**:  
> первая попытка → `retry.30m` → вторая → `retry.2h` → третья → `dlq`.

---

## Топология очередей

Топология описана в файле [`infra/rabbitmq/definitions.json`](../../infra/rabbitmq/definitions.json) и загружается в RabbitMQ **без скриптов** при старте через `docker-compose`.

Все очереди — **durable**, объявлены один раз и идемпотентны.

| Очередь                                | Назначение                                               |
| -------------------------------------- | -------------------------------------------------------- |
| `notifications`                        | Основная очередь для обработки                           |
| `notifications.retry.router`           | Промежуточная очередь для маршрутизации retry            |
| `notifications.retry.30m`              | Задержка 30 минут → возврат в `notifications`            |
| `notifications.retry.2h`               | Задержка 2 часа → возврат в `notifications`              |
| `notifications.dlq`                    | Окончательная DLQ для проваленных уведомлений            |
| `notifications.retry.router.dlq`       | DLQ для ошибок маршрутизации в `retry-service`           |

---

## Связи и маршрутизация

- `notifications` → при `nack` → `notifications.retry.router`  
  (`x-dead-letter-routing-key: notifications.retry.router`)
- `notifications.retry.30m` → по TTL → `notifications`  
  (`x-dead-letter-routing-key: notifications`)
- `notifications.retry.2h` → по TTL → `notifications`
- `notifications.retry.router` → при `nack` → `notifications.retry.router.dlq`
- `notifications.dlq` и `notifications.retry.router.dlq` — **без consumer’ов**, только для алертинга и анализа.

> ⚠️ **Две DLQ**:  
> - `notifications.dlq` — бизнес-ошибки (канал недоступен, невалидные данные),  
> - `notifications.retry.router.dlq` — инфраструктурные ошибки `retry-service` (сбой маршрутизации).

---

## Формат сообщения

Сообщение — JSON-сериализованный объект `Notification` из `@notification-platform/shared`.

При публикации в `notifications.retry.router` **изначально** устанавливается заголовок:

| Заголовок           | Тип      | Описание                                      |
|--------------------|----------|-----------------------------------------------|
| `x-retry-count`    | `number` | Номер попытки (0 — первая)                    |

Последующие попытки инкрементируют это значение.

---

## Поведение компонентов

### `notification-service`
- Использует `createBatchConsumer` из `@notification-platform/queues`.
- Читает из `notifications`.
- При ошибке → вызывает `nack(false, false)` → сообщение уходит в `notifications.retry.router`.
- Успешные → `ack`.

### `retry-service`
- Использует `createRetryConsumer` из `@notification-platform/queues`.
- Читает из `notifications.retry.router`.
- Увеличивает `x-retry-count`:
  - `1` → публикует в `notifications.retry.30m`,
  - `2` → в `notifications.retry.2h`,
  - `≥3` → в `notifications.dlq`.
- При ошибках маршрутизации → `nack` → сообщение попадает в `notifications.retry.router.dlq`.

### Очереди с TTL
- `notifications.retry.30m` и `notifications.retry.2h` — **не имеют consumer’ов**.
- Используют `x-message-ttl` + `x-dead-letter-*` для автоматического возврата в `notifications`.

---

## Загрузка конфигурации

Конфигурация RabbitMQ загружается напрямую из `infra/rabbitmq/definitions.json` при запуске контейнера:

Это:
- не требует выполнения миграций или скриптов,
- согласуется с принципом **«инфраструктура как код»**,
- позволяет применять топологию на любом окружении (dev, prod) без изменения кода.

---

## Перспективы

В будущем логика `retry-service` может быть:
- обобщена для нескольких микросервисов,
- вынесена в отдельную библиотеку,
- масштабирована независимо.

Но текущая реализация уже обеспечивает **четкое разделение ответственности** и **максимальную отказоустойчивость**.

---

## Преимущества

✅ **Нет потерь**: все ошибки — в DLQ.  
✅ **Экспоненциальная задержка**: 30 м → 2 ч.  
✅ **Чёткое разделение**:  
 – `notification-service` — бизнес-логика доставки,  
 – `retry-service` — инфраструктурная маршрутизация.  
✅ **Расширяемость**: легко добавить новые этапы retry через `definitions.json`.  
✅ **Инкапсуляция**: вся логика очередей — в `@notification-platform/queues`.