# Телеметрия

Сервис уведомлений обеспечивает полную наблюдаемость (observability) через **структурированное логирование**, **сквозную корреляцию** и **централизованный сбор телеметрии** с помощью стандарта **OpenTelemetry (OTel)**.

Архитектура телеметрии построена на следующих принципах:

- **Стандартизация**: Использование OpenTelemetry как единого стандарта для логов, трейсов и метрик.
- **Независимость от бэкенда**: Приложение не зависит от системы хранения данных — всё решается на уровне OTel Collector.
- **Чистота бизнес-логики**: Логирование осуществляется через унифицированный интерфейс `Logger`, который не засоряет доменный код деталями реализации.
- **Полная корреляция**: Все события связываются с `traceId` и `spanId`, что позволяет отслеживать жизненный цикл уведомления в распределённой системе.
- **Отказоустойчивость**: Даже при недоступности внешних систем телеметрия остаётся доступной через стандартный вывод (`stdout/stderr`).

---

## Архитектура телеметрии

1. **Логирование в приложении**:
   - Все события записываются через `winston` с использованием кастомного `Logger`.
   - В основе — интерфейс `Log`, содержащий только высокоуровневые поля.

2. **Интеграция с OpenTelemetry**:
   - `@opentelemetry/winston-transport` автоматически добавляет OTel-атрибуты (`traceId`, `spanId`) и отправляет логи в OTLP-формате.
   - SDK автоматически инструментирует HTTP-вызовы и Express-роутинг.

3. **Экспорт данных**:
   - В production: логи и трейсы экспортируются в **OTel Collector** по протоколу OTLP/HTTP.
   - В development: логи выводятся в консоль в читаемом формате для удобства отладки.

4. **Агрегация и визуализация**:
   - OTel Collector обогащает, фильтрует и направляет данные в системы хранения.

> 💡 Схема:  
> `Logger → winston → OTLP Exporter → OTel Collector → Grafana`

---

### Интерфейс `Log`

Бизнес-логика оперирует минимальным интерфейсом, абстрагированным от деталей реализации:

```ts
interface Log {
  message: string;
  eventType?: EventType;
  duration?: number;
  details?: unknown;
  error?: unknown;
}
```

Все остальные поля (`level`, `serviceName`, `timestamp`, `traceId` и др.) **добавляются автоматически** на уровне логгера и OpenTelemetry SDK.

---

## Формат лога

Финальный структурированный лог содержит два типа полей: **атрибуты (attributes)** и **тело (body)**, согласно спецификации OpenTelemetry Logs.

### Атрибуты (Attributes)

| Поле                     | Тип / Значение                                      | Источник                                               |
| ------------------------ | --------------------------------------------------- | ------------------------------------------------------ |
| `level`                  | `LogLevel` (`info`, `warning`, `error`, `critical`) | Определяется методом `logger.info()`, `error()` и т.д. |
| `service.name`           | `string`                                            | Указан в конфигурации OTel SDK                         |
| `service.version`        | `string`                                            | Указан в конфигурации OTel SDK                         |
| `deployment.environment` | `EnvironmentType` (`dev`, `stage`, `prod`)          | Передаётся в конфигурации SDK                          |
| `host.name`              | `string`                                            | Автоматически из `os.hostname()`                       |
| `server.address`         | `string`                                            | Хост сервиса                                           |
| `server.port`            | `number`                                            | Порт сервиса                                           |
| `trigger`                | `TriggerType` (`api`, `cron`, `manual`)             | Жёстко задан как `api`                                 |
| `eventType`              | `EventType` (`bootstrap`, `shutdown`, и т.д.)       | Передаётся в `Log.eventType`                           |
| `trace_id`               | `string` (hex)                                      | Генерируется OTel SDK                                  |
| `span_id`                | `string` (hex)                                      | Генерируется OTel SDK                                  |
| `id`                     | `string` (UUID)                                     | Генерируется внутри `createLogger`                     |

---

### Тело (Body)

| Поле       | Тип / Описание                                                                             |
| ---------- | ------------------------------------------------------------------------------------------ |
| `message`  | Краткое человекочитаемое описание события                                                  |
| `duration` | Длительность операции в миллисекундах (опционально)                                        |
| `details`  | Дополнительные данные (например, сериализованный объект уведомления)                       |
| `error`    | Полная сериализованная ошибка (включая stack trace), если уровень — `error` или `critical` |

---

### Системные поля

| Поле              | Тип / Описание                                                       |
| ----------------- | -------------------------------------------------------------------- |
| `timestamp`       | `Unix timestamp` в наносекундах — добавляется автоматически OTel SDK |
| `severity_text`   | Человекочитаемый уровень (`INFO`, `ERROR` и т.д.) — из `level`       |
| `severity_number` | Цифровой уровень важности — соответствует `logLevels` в `winston`    |

---

## Визуализация (Grafana)

Дашборды доступны в **Grafana**.

### Возможности:

- Поиск логов по:
  - `service.name = "notification-service"`
  - `eventType`, `trigger`, `level`
  - `trace_id` — для сквозной диагностики запроса
- Анализ ошибок с группировкой по `error.message` и `stack_trace`
- Корреляция логов с трейсами через `trace_id`
- Отображение длительности операций (`duration`)
- Просмотр контекста (`details`) в виде JSON

---

## Алертинг

- События уровня `error` и `critical` в **production** автоматически генерируют алерты.
- Алерты отправляются в **Telegram** через бота.
- В сообщении указываются:
  - Уровень и тип события;
  - `trace_id` / `span_id`;
  - Краткое описание ошибки;
  - Ссылка на Grafana для детального анализа.

> ⚠️ Алертинг **отключён** в `dev` и `stage`.

---

## Рекомендации

- ✅ Всегда передавайте значимые данные в `details` — они попадут в лог и помогут в отладке.
- ✅ Используйте `eventType` для категоризации событий (например, `notification.sent`, `rabbitmq.published`).
- ❌ Не логируйте чувствительные данные (пароли, персональные данные) в `details` или `error`.
- ✅ Для измерения времени используйте `duration` — это позволяет строить SLI/SLO по времени обработки.

---

## Интеграции

| Система            | Назначение                                                   |
| ------------------ | ------------------------------------------------------------ |
| `winston`          | Библиотека для записи структурированных логов                |
| `@opentelemetry/*` | SDK для генерации трейсов, атрибутов и OTLP-экспорта         |
| OTel Collector     | Централизованный сбор, обогащение и маршрутизация телеметрии |
| Grafana            | Визуализация логов, трейсов и метрик                         |
| Telegram           | Алертинг по критическим событиям                             |

> 💡 Логирование осуществляется через унифицированный `Logger`, реализующий интерфейс `Log`.  
> Это позволяет легко заменять или расширять поведение без изменений в бизнес-логике.
