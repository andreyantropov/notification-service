# Архитектура проекта

Этот документ описывает концептуальную архитектуру сервиса уведомлений — его цели, структуру, ключевые принципы и роли компонентов.

Он предназначен для:

- новых разработчиков,
- архитекторов,
- DevOps,
- тех, кто интегрирует сервис в другие системы.

> 💡 Документ фокусируется на **"почему" и "как в целом"**, а не на деталях реализации.  
> Подробности — в соответствующих файлах `docs/`.

## Цель проекта

Сервис обеспечивает надёжную, типобезопасную и гибкую отправку уведомлений пользователям через доступные каналы — на текущий момент Email и Bitrix24 API.

Отправка управляется стратегиями доставки (например, «отправить в первый доступный канал» или «отправить во все доступные»), что позволяет независимо от кода менять поведение сервиса через конфигурацию.

Архитектура изначально спроектирована с расчётом на расширение: подключение новых каналов (SMS, Push и др.) не требует переработки ядра — достаточно реализовать новый адаптер и зарегистрировать его в DI-контейнере.

## Структура проекта

````src/
├── application/ # Прикладной уровень: бизнес-логика
│ ├── ports/ # Порты
│ ├── enums/ # Enum'ы
│ ├── services/ # Сервисы
│ ├── types/ # Типы
│ └── useCases/ # UseCase'ы
├── composition/ # Composition Root
│ ├── bootstrap/ # Запуск приложения
│ ├── container/ # DI-контейнер для управления зависимостями
│ ├── lifecycle/ # Жизненный цикл приложения
│ ├── telemetry/ # Телеметрия
│ └── types/ # Типы
├── configs/ # Конфиги инфраструктурных сущностей
├── domain/ # Доменная модель
│ ├── constants/ # Константы
│ ├── enums/ # Enum'ы
│ ├── ports/ # Порты
│ └── types/ # Типы
├── infrastructure/ # Инфраструктурные клиенты
│ ├── channels/ # Каналы рассылки уведомлений
│ ├── generators/ # Генераторы
│ ├── http/ # Зависимости от http-фреймворков
│ ├── queues/ # Очереди
│ └── telemetry/ # Телеметрия
├── presentation/ # Интерфейс пользователя (Swagger)
├── shared/ # Общие абстракции и утилиты
└── index.ts # Точка входа приложения

## Архитектурные принципы

Архитектура сервиса построена вокруг четырёх ключевых идей:

1. **Чистая архитектура**
   Бизнес-логика (`domain`) полностью независима от инфраструктуры. Внешние зависимости (HTTP, очереди, каналы) подключаются через порты и адаптеры.

2. **Расширяемость через DI и стратегии**
   Поведение сервиса (например, выбор канала доставки) определяется стратегиями, инжектируемыми через DI-контейнер. Это позволяет добавлять новые каналы или менять логику без изменения ядра.

3. **Функциональный стиль**
   Компоненты реализованы как чистые фабричные функции с явными зависимостями. Минимум состояния, максимум предсказуемости и тестируемости.

4. **Следование 12-Factor**
   Сервис stateless, конфигурируется через переменные окружения, пишет логи в stdout и готов к развёртыванию в облачных средах с CI/CD.

## Компоненты и взаимодействие

### 1. Сервис уведомлений (Notification Service)
- **Роль**: Принимает запросы на отправку уведомлений по HTTP API и доставляет их через настраиваемые каналы (Email, Bitrix24).
- **Особенности**:
  - Поддерживает синхронную и асинхронную (через очередь) отправку.
  - Обеспечивает retry-логику и dead-letter-очередь для надёжной доставки.
  - Расширяем: новые каналы подключаются без изменения ядра.

### 2. Внешние каналы доставки
- **Email** — через SMTP-сервер.
- **Bitrix24** — через REST API.
- Каналы изолированы от бизнес-логики через порты и адаптеры.

### 3. Очередь сообщений (RabbitMQ)
- **Роль**: Внутренний буфер для отложенной и повторной отправки уведомлений.
- Является частью инфраструктурного уровня сервиса, а не внешней системой.

### 4. Системы мониторинга (опционально)
- Телеметрия (логи, метрики, трейсы) экспортируется в OpenTelemetry Collector для агрегации и анализа.
- Не участвует в функциональном потоке — используется только для наблюдаемости.

## Диаграмма компонентов

```mermaid
flowchart LR
    Client1[Client 1] --> NS[Notification Service]
    Client2[Client 2] --> NS
    Client3[Client 3] --> NS

    NS --> Queue[(RabbitMQ)]

    Queue --> NS

    NS --> SMTP[SMTP]
    NS --> Bitrix24[Bitrix24]

    SMTP --> User1((User 1))
    Bitrix24 --> User1

    SMTP --> User2((User 2))
````

## Описание процесса работы

Сервис поддерживает два режима обработки уведомлений:

- **Срочные** (`isImmediate: true`) — обрабатываются синхронно и отправляются немедленно.
- **Несрочные** — помещаются в очередь сообщений (RabbitMQ) для асинхронной доставки с гарантией повторных попыток и dead-letter-обработки.

В обоих случаях клиенту **немедленно возвращается подтверждение приёма** (`HTTP 202 Accepted` или `HTTP 207 Multi-Status`), что позволяет системе быть отзывчивой даже при высокой нагрузке или временных сбоях в каналах.

Фактическая доставка — **асинхронна и надёжна**: ошибки при отправке автоматически повторяются, а проблемные сообщения изолируются для последующего анализа.

## API сервиса

Сервис предоставляет HTTP API для отправки уведомлений — как по одному, так и пакетами (до 100 за запрос).  
Все запросы защищены **JWT-аутентификацией** (требуется валидный токен Keycloak).

> Полная спецификация API (форматы, статусы, примеры) вынесена в отдельный документ: [`api.md`](./api.md).

## Очередь сообщений

Для надёжной асинхронной обработки несрочных уведомлений используется очередь сообщений.  
Это обеспечивает отказоустойчивость, буферизацию при пиковой нагрузке и гарантию доставки даже при сбоях сервиса.

> Подробности топологии, retry-логики и DLQ — в [`docs/message-queue.md`](./message-queue.md).

## Стратегия доставки уведомлений

Поведение отправки уведомлений определяется **настраиваемой стратегией**, задаваемой в каждом запросе.  
Это позволяет гибко управлять доставкой — от отправки в первый доступный канал до параллельной рассылки во все.  
Новые стратегии можно добавлять без изменения ядра сервиса.

> Подробности реализации — в [`docs/strategies.md`](./strategies.md).

## Телеметрия

Сервис обеспечивает наблюдаемость через **логи, метрики и распределённые трейсы**, собранные по стандарту **OpenTelemetry**.  
Сбор телеметрии **полностью отделён от бизнес-логики** и не влияет на поведение ядра.  
Данные экспортируются в централизованный **OTel Collector**, который отвечает за маршрутизацию и агрегацию.

> Подробности метрик, атрибутов и форматов — в [`docs/telemetry.md`](./telemetry.md).

## Composition Root

Всё приложение собирается в единой точке — **Composition Root**, расположенной на границе инфраструктурного слоя.  
Здесь зависимости (каналы, логгеры, адаптеры) инжектируются в use cases и сервисы, формируя готовый к запуску HTTP-сервер.

Этот подход обеспечивает чёткое разделение слоёв, исключает жёсткую связанность и централизует управление жизненным циклом компонентов.

> Подробности — в [`docs/composition-root.md`](./composition-root.md).

## Перспективы развития

Архитектура сервиса спроектирована с учётом поэтапного развития. Ниже — ключевые направления, которые уже заложены в текущую структуру или находятся в планах:

### 1. Оркестрация через Kubernetes

- Текущая контейнеризация (Docker) позволяет легко перейти к управлению через Kubernetes.
- Сервис уже соответствует требованиям к stateless-приложениям, healthcheck’ам (`/live`, `/ready`) и логированию в `stdout` — что упрощает интеграцию с K8s.

### 2. Выделение Retry-логики в отдельный сервис

- Сейчас retry-поведение реализовано внутри `Notification Service`.
- В будущем планируется вынести логику повторных попыток в **отдельный, обобщённый retry-сервис**, который:
  - будет получать задачи через очередь,
  - выполнять экспоненциальные backoff’ы и DLQ-обработку,
  - возвращать результат обратно в очередь или в callback-адрес.
- Это позволит **полностью исключить retry-логику из ядра уведомлений**, сделав его ещё более «глупым» и сфокусированным только на доставке.

### 3. Поддержка новых каналов и триггеров

- Архитектура уже позволяет добавлять новые каналы (SMS, Push и др.) без изменения ядра.
- В перспективе — поддержка **входящих триггеров** (например, вебхуки от внешних систем), что потребует расширения `presentation`-слоя, но не затронет `domain`.

### 4. Централизованный рейт-лимитинг через Redis

- Текущая реализация `RateLimiterMiddleware` использует in-memory хранилище, что ограничивает её эффективность при горизонтальном масштабировании (каждая реплика имеет свой собственный счётчик).
- В перспективе планируется замена на **распределённый рейт-лимитер на базе Redis**, который:
  - будет обеспечивать **единые лимиты на уровне всего сервиса**, независимо от количества инстансов,
  - поддержит гибкие стратегии (например, sliding window, token bucket),
  - сохранит совместимость с текущим API middleware — без изменений в `application` и `domain`.

> Эти направления не требуют радикальной перестройки — они возможны благодаря текущему разделению ответственности и строгому следованию принципам Clean Architecture и 12-Factor.
