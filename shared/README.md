# Shared Kernel

Этот пакет содержит **Shared Kernel** (в терминах Domain-Driven Design) для bounded contexts:  
- `notification-service`  
- `retry-service`

## Что здесь можно найти

- **`domain/`** — общие бизнес-сущности и правила (`Notification`, `Contact`, `ChannelType`).  
- **`application/`** — порты, DTO и типы, от которых зависят use-case’ы и их обвязка (`Logger`, `Log`, `LogLevel`, `Producer`).  
- **`infrastructure/`** — абстракции инфраструктурного уровня, необходимые для согласованного поведения сервисов (например, интерфейс `Server` и связанные типы).  

## 🔒 Правила внесения изменений

1. **Не добавляйте реализации** — только типы, интерфейсы, enum’ы и pure-функции.  
2. **Не добавляйте зависимости** — `shared` должен быть свободен от runtime-зависимостей.  
3. **Изменения здесь затрагивают все сервисы** — проводите их осознанно и согласовывайте с командой.  
4. **Если сомневаетесь — не выносите в `shared`**. Лучше дублировать, чем засорить ядро.

## 🛠️ Доступные скрипты

Пакет включает стандартный набор скриптов для поддержания качества кода:

- `build` — компиляция TypeScript-кода (`tsc`).  
- `test` — запуск unit-тестов в watch-режиме (`vitest`).  
- `test:coverage` — запуск тестов с генерацией отчёта о покрытии.  
- `lint` — проверка кода с помощью ESLint.  
- `lint:fix` — автоматическое исправление линтинговых ошибок.  
- `lint:format` — форматирование кода с помощью Prettier.  
- `typecheck` — проверка типов без генерации выходных файлов (`tsc --noEmit`).  

Эти скрипты помогают поддерживать `shared` в согласованном и безопасном состоянии, особенно учитывая, что изменения здесь влияют на все зависимые сервисы.