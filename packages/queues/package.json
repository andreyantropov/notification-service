{
  "name": "@notification-platform/queues",
  "version": "1.0.0",
  "description": "Реализации и декораторы для работы с очередями сообщений (RabbitMQ) в @notification-platform.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint --ext .ts,.tsx .",
    "lint:fix": "eslint --fix --ext .ts,.tsx .",
    "lint:format": "prettier . --write",
    "typecheck": "tsc --noEmit"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@notification-platform/shared": "^1.0.0",
    "@cloudamqp/amqp-client": "^3.3.2",
    "p-timeout": "^6.1.4"
  }
}