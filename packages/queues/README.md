# Queues

Реализации и декораторы для работы с очередями сообщений (RabbitMQ) в платформе уведомлений.

> ⚠️ **Этот пакет не управляет подключением к RabbitMQ напрямую.**  
> Он предоставляет **готовые к использованию реализации** `Producer` и `Consumer`, а также **декораторы с логированием**.

## 📦 Содержимое

### 📤 Producer
- **`createProducer`** — базовая реализация `Producer`:
  - публикация сообщений в очередь,
  - поддержка таймаутов и healthcheck'ов,
  - durable-очереди, persistent-сообщения.

### 📥 Consumer
- **`createBatchConsumer`** — consumer с буферизацией и пакетной обработкой:
  - группировка сообщений в батчи,
  - гибкая настройка размера и времени сброса,
  - обработка с подтверждением (`ack`/`nack`) по результату.
- **`createRetryConsumer`** — consumer для маршрутизации сообщений с повторами:
  - инкремент счётчика `x-retry-count`,
  - определение целевой очереди на основе стратегии повтора.

### 🎯 Декораторы
- **`createLoggedProducer`** — добавляет структурированное логирование к любому `Producer`.
- **`createLoggedConsumer`** — добавляет структурированное логирование к любому `Consumer`.

Все операции (в реализациях и декораторах) поддерживают:
- **единый формат логов** (`traceId`, `spanId`, `service.name`, `level`, `timestamp`),
- **типизированные события** (`EventType.Bootstrap`, `EventType.MessagePublish`),
- **длительность выполнения** (`durationMs`),
- **автоматическую сериализацию ошибок**.

## 🧩 Требования

- **RabbitMQ** должен быть доступен по указанному URL.
- Для декораторов требуется `Logger` из `@notification-platform/shared`.

## 🚀 Использование

### Базовое использование (реализации)
```ts
// В вашем сервисе
import { createProducer, createBatchConsumer } from '@notification-platform/queues';
import type { Notification } from '@notification-platform/shared';

const producer = createProducer<Notification>({
  url: 'amqp://localhost',
  queue: 'notifications',
});

const consumer = createBatchConsumer<Notification>(
  { handler: async (items) => { /* ... */ } },
  { url: 'amqp://localhost', queue: 'notifications' }
);

await producer.start();
await consumer.start();

await producer.publish([{ id: '1', type: 'email' }]);
```

### С логированием (декораторы)
```ts
import { createLoggedProducer } from '@notification-platform/queues';

const loggedProducer = createLoggedProducer({
  producer: createProducer<Notification>({ /* ... */ }),
  logger,
});
```

## 📁 Архитектура

- **Зависит от**:  
  `@notification-platform/shared` (порты `Producer`, `Consumer`),  
  `@cloudamqp/amqp-client` (реализация RabbitMQ).
- **Следует принципам**:  
  - _«Сделай одну вещь и сделай её хорошо»_ — каждая реализация решает одну задачу,  
  - _«Открыт для расширения»_ — легко обернуть в декораторы (логирование, трассировка, метрики).

## 🛠️ Доступные скрипты

Пакет включает стандартный набор скриптов для локальной разработки и обеспечения качества:

- `build` — компиляция TypeScript-кода (`tsc`).  
- `test` — запуск unit-тестов в watch-режиме (`vitest`).  
- `test:coverage` — запуск тестов с генерацией отчёта о покрытии.  
- `lint` — проверка кода с помощью ESLint.  
- `lint:fix` — автоматическое исправление линтинговых ошибок.  
- `lint:format` — форматирование кода с помощью Prettier.  
- `typecheck` — проверка типов без генерации выходных файлов (`tsc --noEmit`).  

Эти скрипты помогают поддерживать стабильность и предсказуемость поведения компонентов при интеграции в `notification-service` и `retry-service`.